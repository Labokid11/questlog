const plusThemes = [['aurora', 'Aurora'], ['velvet', 'Velvet']];
const plusDialog = document.createElement('dialog'); plusDialog.className = 'plus-dialog'; plusDialog.innerHTML = `<form method="dialog"><button class="close" value="cancel">×</button><p class="eyebrow lime">QUESTLOG PLUS</p><h2>Make it yours.</h2><p class="muted">Unlock premium visual themes and future Plus features.</p><div class="feature-list"><span>Aurora and Velvet themes</span><span>Future profile customisation</span><span>Support Questlog’s development</span></div><button class="primary" value="default">Preview Plus</button></form>`; document.body.append(plusDialog);
const picks = document.querySelector('.theme-picks');
plusThemes.forEach(([theme, label]) => { const button = document.createElement('button'); button.className = 'theme-choice locked'; button.dataset.theme = theme; button.innerHTML = `<i></i>${label}`; picks.append(button); button.addEventListener('click', () => { if (document.body.dataset.plusMember === 'true') { document.body.dataset.theme = theme; document.querySelectorAll('.theme-choice').forEach(choice => choice.classList.toggle('selected', choice === button)); localStorage.setItem('questlog-theme', theme); return; } plusDialog.showModal(); }); });
const accountTools = document.querySelector('.account-tools'); if (accountTools) { const card = document.createElement('div'); card.className = 'plus-card'; card.innerHTML = `<p class="eyebrow">QUESTLOG PLUS</p><h3>More ways to make it yours.</h3><p>Unlock Aurora, Velvet, and upcoming Plus features.</p><button type="button">Explore Plus</button>`; accountTools.append(card); card.querySelector('button').addEventListener('click', () => plusDialog.showModal()); }
document.querySelectorAll('.profile-trigger').forEach(trigger => trigger.addEventListener('click', () => document.querySelectorAll('.nav-item').forEach(item => item.classList.remove('active'))));
document.querySelectorAll('.platform').forEach(platform => { if (platform.value === 'playstation') platform.querySelector('i').textContent = 'Connect →'; });
